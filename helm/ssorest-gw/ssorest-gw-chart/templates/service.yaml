apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssorest-gw-chart.fullname" . }}
  labels:
    {{- include "ssorest-gw-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8180  # Assuming Tomcat is running on 8080
      protocol: TCP
      name: http
  selector:
    {{- include "ssorest-gw-chart.selectorLabels" . | nindent 4 }}